version: 0.2


phases:
  install:
    runtime-versions:
      python: 3.11
  build:
    commands:
      # Install dependencies
      - pip install -r requirements.txt
 
artifacts:
  files:
    - '**/*'
    